import Section from '../sections/Booking.jsx';

/* One section, on its own page. The section components already carry their own
   heading, padding and background, so the page is just the route target.

   ONE THING TO CHECK. The background photograph ("beach drone.png", supplied
   26 Aug 2026) is not Ekas. Rows of sun loungers, rainbow umbrellas, beach
   clubs along the sand and a telecoms mast behind the trees — that is one of
   the Gilis, or somewhere very like them. Ekas Bay has none of it, and the
   whole pitch of this site is that the villa sits a long way past where the
   day trippers turn around.

   It is used as asked, and the alt text describes what the picture shows
   rather than claiming it is the villa's beach. But a guest who books on the
   strength of this image and arrives at a quiet bay has been sold the wrong
   holiday, so it is worth a second look before launch. */
export default function AvailabilityPage() {
  return <Section />;
}
